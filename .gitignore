# Local SDD tooling cache (regenerable via `gentle-ai skill-registry refresh`)
.atl/

# Database backups (pg_dump artifacts, not for version control)
backend/*.backup

# CodeGraph local index (regenerable, machine-local)
.codegraph/

# Build/lint/test log artifacts (regenerable via local commands)
*.out

# Local editor configuration (machine-local, not for version control)
.vscode/